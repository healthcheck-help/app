name: Version

on:
  push:
    branches:
      - main

jobs:
  version:
    name: Version
    if: ${{ !contains(github.event.head_commit.message, 'from changeset-release/') }}
    runs-on: srv-obin-ch_ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Deno
        uses: https://github.com/denoland/setup-deno@v2
        with:
          deno-version-file: .tool-versions

      - name: Cache dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.deno
            ~/.cache/deno
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
          restore-keys: |
            ${{ runner.os }}-deno-

      - name: Install dependencies
        run: deno install

      - name: Create Release Pull Request
        uses: https://github.com/openscript/changesets-action@v1.9.0
        id: changesets
        with:
          version: deno task version
          commit: "chore: publish new release"
          title: "chore: publish new release"
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
